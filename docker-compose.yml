version: '3.9'

services:
  lite-lms:
    container_name: podium-app
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - ".env.local"
