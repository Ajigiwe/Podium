{
    "rules": {
        "chats": {
            "$sessionId": {
                ".read": "auth != null",
                ".write": "auth != null",
                "$messageId": {
                    ".validate": "newData.hasChildren(['userId', 'userName', 'userRole', 'content', 'createdAt'])"
                }
            }
        }
    }
}